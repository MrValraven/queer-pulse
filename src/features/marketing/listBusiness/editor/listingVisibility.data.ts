/**
 * What a pause KEEPS, listed out.
 *
 * Owners were reaching for delete when what they wanted was a pause, and a
 * delete takes the reviews with it. So the control leads with everything that
 * stays exactly where it is, rather than with everything that stops.
 */
export const VISIBILITY_KEPT_KEYS = [
  "marketing:listBusiness.visibility.kept.reviews",
  "marketing:listBusiness.visibility.kept.content",
  "marketing:listBusiness.visibility.kept.reversible",
];
