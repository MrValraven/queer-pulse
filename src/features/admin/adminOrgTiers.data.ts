import type { OrgTierAdminDTO } from "../marketing/api/adminOrgTiers.api";

/**
 * Admin org-tiers demo fixture. Deliberately empty: the admin panel's demo
 * mode shows the honest "nothing to manage yet" empty state rather than
 * fabricating tiers as platform truth (mirrors adminHousing.data.ts).
 */
export const ADMIN_ORG_TIERS_DEMO: OrgTierAdminDTO[] = [];
