// ── Motion shared by the composer's rail blocks ─────────────────────────────

/** The `--ease` token as a cubic-bezier, so a motion animation and a CSS
 *  transition sitting side by side move with the same spring. */
export const COMPOSE_EASE = [0.22, 0.68, 0.16, 1] as const;
