// Re-export of the canonical hook, kept so existing economy imports keep working.
// Prefer importing from '../../../shared/hooks' in new code.
export { useLocalStorage } from '../../../shared/hooks/useLocalStorage'
